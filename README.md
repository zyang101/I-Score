# I-Score

Instructions:

1.) Make sure the version is Python 3.10 in order to install some of the dependencies

2.) Install all the packages in the requirements.txt, use the command "pip install -r requirements.txt"

3.) Run through each of the cells on the notebook until the end. This may require some waiting.

4.) The output video will be stored in the output_videos folder for viewing.
